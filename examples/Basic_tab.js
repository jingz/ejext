 
Basic_tab = Ext.extend(Basic_tabUI,{
  constructor: function  (config) {
    config = config || {};
    Ext.apply(this, config);
    Basic_tab.superclass.constructor.call(this)
  },
  
  initComponent: function(){
    Basic_tab.superclass.initComponent.call(this);
    var self = x = this;
    // use x for debugging at console
    
    
  }
});
